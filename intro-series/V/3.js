// Create a function that takes a single number as a parameter.
// The function should return true if that number is even, and false if not.
// (Hint: use the modulus, and remember threequals for the win!)
let isEven = (n) => {
  return n%2 === 0;
};