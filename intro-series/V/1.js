// Create a variable that holds the string “Hello” and another that holds the string “World.”
var hello = "Hello";
var world = "World";

// Now create a console.log statement that concatenates the two strings.
console.log(hello + " " + world);